<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BookD</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        .card {
            background: var(--bs-light);
            border-radius: 8px;
            padding: 20px;
        }
        .form-control {
            border-radius: 0;
            border-color: var(--bs-gray-dark);
        }
        .form-label {
            font-weight: 500;
            color: var(--bs-black);
        }
        .btn-primary {
            background-color: var(--bs-primary);
            border: none;
            
        }
        .btn-primary:hover {
            background-color: var(--bs-black);
        }
        .error-message{
            color: red; /* Example styling */
    font-size: 14px;
    display: none; 

        }
        .custom-form {
    text-align: center;
}

.custom-form button {
    display: block;
    margin: 10px auto;
    width: 50%;
}

    </style>
</head>
<body>
    <div class="container d-flex justify-content-center align-items-center min-vh-100">
        <div class="col-md-6">
            <div class="card shadow-sm border-0">
                <h2 class="text-center mb-3">Change Email</h2>
                <% if (message) { %>
                    <div class="alert alert-warning text-center">
                        <%= message %>
                    </div>
                <% } %>
                                    <!-- Form to Request OTP -->
            <form action="/profile/change-email" method="POST" class="custom-form">
                <label class="form-label">New Email</label>
                <input type="email" name="newEmail" class="form-control" placeholder="Enter new email" required>
    
                <button type="submit" class="btn btn-primary w-50 mt-3">Request OTP</button>
            </form>
      
               
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
       
    </script>
</body>
</html>
